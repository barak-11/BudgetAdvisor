<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <item android:id="@+id/menu_add"
          android:title="Add"
        android:icon="@drawable/ic_new"
        app:showAsAction="always"/>

    <item android:id="@+id/menu_save"
        android:title="Save"
        android:icon="@drawable/ic_save"
        app:showAsAction="always"/>

    <item android:id="@+id/menu_remove"
        android:title="Remove"
        android:icon="@drawable/ic_remove"
        app:showAsAction="always"/>
    <item android:id="@+id/menu_graph"
        android:title="Graphs"
        android:icon="@drawable/ic_graph"
        app:showAsAction="never"/>
    <item android:id="@+id/menu_setBudget"
        android:title="Set Budget"
        android:icon="@drawable/ic_settings"
        app:showAsAction="never"/>
    <item android:id="@+id/menu_setProject"
        android:title="Select Proejct"
        android:icon="@drawable/ic_settings"
        app:showAsAction="never"/>
</menu>